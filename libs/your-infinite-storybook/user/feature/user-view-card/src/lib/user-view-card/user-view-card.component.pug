ng-container(*ngIf='query$ | async as result')
    your-infinite-storybook-user-ui-user-view-card(
        [user]='user',
        [loading]='loading'
    )
        ng-content(select='[header-button]', header-button)

    ng-content
