ng-container
    div(*ngFor='let user of users')
        your-infinite-storybook-user-ui-user-grid-item(
            [user]='user',
            (userGridItemClick)='onUserGridClick($event)'
        )
