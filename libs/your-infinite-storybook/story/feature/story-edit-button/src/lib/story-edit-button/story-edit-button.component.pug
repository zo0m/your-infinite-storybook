ng-container(*ngIf='{ query: query$ | async } as result')
    your-infinite-storybook-story-ui-story-edit-button(
        [storyId]='storyId',
        [story]='story',
        [placeholder]='placeholder',
        [loading]='loading'
    )
        ng-content
