ng-container(*ngIf='query$ | async as result')
    ng-container(*ngIf='!loading; else loadingTemplate')
        your-infinite-storybook-story-ui-story-grid(
            [stories]='stories',
            (storyGridClick)='onStoryGridClick($event)'
        )

    ng-template(#loadingTemplate)
        p loading
